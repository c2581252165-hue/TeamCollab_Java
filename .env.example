# Backend local configuration. Never commit real values.
DB_USERNAME=root
DB_PASSWORD=replace-with-your-local-database-password
JWT_SECRET=replace-with-a-long-random-secret

# Optional. Leave blank to keep AI features disabled.
DEEPSEEK_API_KEY=